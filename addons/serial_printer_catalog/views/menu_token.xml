<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!-- Menú para Token (bajo Catálogo) -->
        <menuitem
            id="menu_serial_printer_token"
            name="Token"
            parent="menu_serial_printer_catalog_root"
            sequence="40"
        />

        <!-- Acción para mostrar la vista tree de tokens -->
        <record id="action_serial_printer_token" model="ir.actions.act_window">
            <field name="name">Token</field>
            <field name="res_model">serial.printer.token</field>
            <field name="view_mode">list,form</field>
        </record>

        <!-- Elemento de menú que lanza la acción -->
        <menuitem
            id="menu_serial_printer_token_action"
            name="Ver Tokens"
            parent="menu_serial_printer_token"
            action="action_serial_printer_token"
            sequence="10"
        />

    </data>
</odoo>
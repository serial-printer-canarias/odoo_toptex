<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="serial_printer_attribute_list" model="ir.ui.view">
        <field name="name">serial.printer.attribute.list</field>
        <field name="model">serial.printer.attribute</field>
        <field name="arch" type="xml">
            <list string="Lista de atributos">
                <field name="name"/>
                <field name="toptex_id"/>
            </list>
        </field>
    </record>

    <record id="action_serial_printer_attribute" model="ir.actions.act_window">
        <field name="name">Atributos del catálogo</field>
        <field name="res_model">serial.printer.attribute</field>
        <field name="view_mode">list</field>
    </record>
</odoo>
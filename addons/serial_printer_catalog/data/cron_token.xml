<odoo>
    <data noupdate="1">

        <!-- Acción servidor para renovar el token -->
        <record id="ir_cron_renew_toptex_token" model="ir.cron">
            <field name="name">Renovar token TopTex</field>
            <field name="model_id" ref="model_serial_printer_token"/>
            <field name="state">code</field>
            <field name="code">model.renew_token()</field>
            <field name="interval_number">50</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
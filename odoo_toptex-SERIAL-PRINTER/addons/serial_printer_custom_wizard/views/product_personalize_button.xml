<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data name="SPW: Botón Personalizar (Product Page)" inherit_id="website_sale.product">
    <!-- Inserta el botón dentro del <form> que SIEMPRE contiene el hidden product_id -->
    <xpath expr="//form[.//input[@name='product_id']]" position="inside">
      <div class="mt-3">
        <a id="spw_personalize_btn"
           class="btn btn-primary w-100"
           t-att-data-template-id="(product.product_tmpl_id and product.product_tmpl_id.id) or (product_template and product_template.id) or 0"
           t-att-data-product-id="product and product.id"
           t-att-href="'/spw/customize/%s?variant_id=%s' % (((product.product_tmpl_id and product.product_tmpl_id.id) or (product_template and product_template.id) or 0), (product and product.id) or 0)">
          <i class="fa fa-magic me-1"/>
          <span>Personalizar</span>
        </a>
      </div>
    </xpath>
  </data>
</odoo>
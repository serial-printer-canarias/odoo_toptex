<odoo>
  <template id="spw_personalize_btn"
            name="SPW: Botón Personalizar (Product Page)"
            inherit_id="website_sale.product">

    <!-- Inserta el botón dentro del <form> que contiene product_id -->
    <xpath expr="//form[.//input[@name='product_id']]" position="inside">
      <div class="mt-3">
        <a id="spw_personalize_btn"
           class="btn btn-primary w-100"
           t-attf-href="/spw/customize/#{product.product_tmpl_id.id}?variant_id=#{product.id}">
          <i class="fa fa-magic me-1"/>
          <span>Personalizar</span>
        </a>
      </div>
    </xpath>
  </template>
</odoo>
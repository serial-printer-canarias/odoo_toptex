<?xml version="1.0" encoding="UTF-8"?>
<odoo>

  <!-- ============ PÁGINA DEL PERSONALIZADOR ============ -->
  <template id="spw_customize_page" name="SPW - Customizer Page">
    <t t-call="website.layout">
      <t t-set="pageName" t-value="'spw_customize_page'"/>
      <t t-set="main_object" t-value="template"/>

      <div class="container my-5 spw-customizer">
        <input type="hidden" id="spw_template_id" t-att-value="template.id"/>
        <input type="hidden" id="spw_variant_id" t-att-value="variant_id or ''"/>
        <input type="hidden" id="spw_img_src" t-att-value="img_src"/>

        <div class="row g-4">
          <div class="col-12 col-lg-7">
            <div id="spw_canvas" class="spw-stage card p-3" style="position:relative;">
              <!-- Base same-origin para evitar CORS al exportar -->
              <img id="spw-base-img"
                   t-att-src="'/web/image/product.product/%s/image_1920' % (variant_id or (template.product_variant_id and template.product_variant_id.id) or 0)"
                   class="d-none" alt="Base interna Odoo"/>

              <!-- Imagen visible -->
              <img id="spw_product_img" class="spw-base img-fluid rounded"
                   t-att-src="img_src" alt="Producto"/>

              <!-- Overlay del logo -->
              <img id="spw_logo_preview" class="spw-logo-preview d-none" alt="Logo preview"/>
            </div>
          </div>

          <div class="col-12 col-lg-5">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title mb-3">Personalización</h5>

                <!-- Subir logo -->
                <div class="mb-3">
                  <label class="form-label">Subir logo (PNG/JPG/SVG)</label>
                  <input id="spw_logo_input" class="form-control" type="file"
                         accept=".png,.jpg,.jpeg,.svg,.PNG,.JPG,.JPEG,.SVG"/>
                  <small class="text-muted">Máx. 10MB.</small>
                </div>

                <!-- Técnica -->
                <div class="mb-3">
                  <label class="form-label">Técnica</label>
                  <div class="d-flex gap-3 flex-wrap">
                    <label class="form-check">
                      <input class="form-check-input" type="radio" name="spw_tech" value="Serigrafía" checked="checked"/>
                      <span class="form-check-label">Serigrafía</span>
                    </label>
                    <label class="form-check">
                      <input class="form-check-input" type="radio" name="spw_tech" value="DTF"/>
                      <span class="form-check-label">DTF</span>
                    </label>
                    <label class="form-check">
                      <input class="form-check-input" type="radio" name="spw_tech" value="Bordado"/>
                      <span class="form-check-label">Bordado</span>
                    </label>
                  </div>
                </div>

                <!-- Color SVG -->
                <div class="mb-3">
                  <label class="form-label">Color (SVG)</label>
                  <div class="spw-color-row">
                    <label class="spw-color-swatch is-black">
                      <input type="radio" name="spw_svg_color" value="#000000" checked="checked"/>
                      <span class="spw-color-ring"></span>
                    </label>
                    <label class="spw-color-swatch is-white">
                      <input type="radio" name="spw_svg_color" value="#FFFFFF"/>
                      <span class="spw-color-ring"></span>
                    </label>
                    <label class="spw-color-swatch is-red">
                      <input type="radio" name="spw_svg_color" value="#D62828"/>
                      <span class="spw-color-ring"></span>
                    </label>
                    <label class="spw-color-swatch is-blue">
                      <input type="radio" name="spw_svg_color" value="#2060FF"/>
                      <span class="spw-color-ring"></span>
                    </label>
                    <label class="spw-color-swatch is-green">
                      <input type="radio" name="spw_svg_color" value="#2A9D8F"/>
                      <span class="spw-color-ring"></span>
                    </label>
                  </div>
                  <div class="spw-help mt-1">Se aplica solo a logos SVG.</div>
                </div>

                <!-- Controles -->
                <div class="mb-3">
                  <label for="spw_size" class="form-label">Tamaño</label>
                  <input id="spw_size" class="form-range" type="range" min="10" max="200" value="100"/>
                </div>
                <div class="mb-3">
                  <label for="spw_pos_x" class="form-label">Posición X</label>
                  <input id="spw_pos_x" class="form-range" type="range" min="-50" max="50" value="0"/>
                </div>
                <div class="mb-3">
                  <label for="spw_pos_y" class="form-label">Posición Y</label>
                  <input id="spw_pos_y" class="form-range" type="range" min="-50" max="50" value="10"/>
                </div>
                <div class="mb-3">
                  <label for="spw_rotation" class="form-label">Rotación</label>
                  <input id="spw_rotation" class="form-range" type="range" min="-45" max="45" value="0"/>
                </div>

                <!-- Cantidad / Observaciones -->
                <div class="mb-3">
                  <label for="spw_qty" class="form-label">Cantidad</label>
                  <input id="spw_qty" class="form-control" type="number" min="1" value="1"/>
                </div>
                <div class="mb-3">
                  <label for="spw_notes" class="form-label">Observaciones</label>
                  <textarea id="spw_notes" class="form-control" rows="3"
                            placeholder="Indica instrucciones para el taller..."></textarea>
                </div>

                <!-- Acciones -->
                <div class="d-flex align-items-center gap-2 flex-wrap">
                  <a class="btn btn-outline-secondary" href="javascript:void(0)"
                     onclick="window.spwReset &amp;&amp; window.spwReset();">Reset</a>

                  <button id="spw_add_to_cart" type="button" class="btn btn-primary">
                    Añadir al carrito con esta personalización
                  </button>

                  <button id="spw_download_png" type="button" class="btn btn-light">
                    Descargar PNG
                  </button>

                  <a class="btn btn-link ms-2" t-att-href="'/shop/product/%s' % template.id">
                    &#8592; Volver al producto
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- ====== JS INLINE (iOS download + preview carrito robusto + swatch color) ====== -->
      <script type="text/javascript"><![CDATA[
      (function () {
        "use strict";

        function $(sel, root){return (root||document).querySelector(sel);}
        function qa(sel, root){return Array.from((root||document).querySelectorAll(sel));}
        function $val(id, d){ var el=$("#"+id); return el?el.value:d; }

        async function loadImage(src){
          return new Promise(function(resolve, reject){
            var im=new Image();
            im.onload=function(){ resolve(im); };
            im.onerror=function(e){ reject(e); };
            im.src=src;
          });
        }
        function readAsDataURL(file){
          return new Promise(function(resolve, reject){
            var r=new FileReader();
            r.onload=function(){ resolve(r.result); };
            r.onerror=reject;
            r.readAsDataURL(file);
          });
        }
        function dataURLtoBase64(u){ var i=u.indexOf(','); return i!==-1?u.slice(i+1):u; }

        var S={ logoDataURL:"", isSVG:false };

        function applyTransform(){
          var logo = $("#spw_logo_preview"); if(!logo) return;
          var base = $("#spw_product_img");  if(!base) return;

          var sizePx = parseInt($val("spw_size","100"),10);
          var posX   = parseInt($val("spw_pos_x","0"),10);
          var posY   = parseInt($val("spw_pos_y","10"),10);
          var rot    = parseInt($val("spw_rotation","0"),10);

          var bw = base.clientWidth, bh = base.clientHeight;
          var leftPx = bw*(0.5 + posX/100);
          var topPx  = bh*(0.6 + posY/100);

          logo.style.position="absolute";
          logo.style.left = leftPx + "px";
          logo.style.top  = topPx  + "px";
          logo.style.transform = "translate(-50%, -50%) rotate("+rot+"deg)";
          logo.style.width = sizePx + "px";
          logo.classList.remove("d-none");
          logo.style.opacity = "1";
        }

        async function renderPNGDataURL(){
          var internalBaseEl = $("#spw-base-img");
          var baseSrc = (internalBaseEl && internalBaseEl.getAttribute("src")) ||
                        $val("spw_img_src","") ||
                        ($("#spw_product_img")||{}).getAttribute("src");
          if(!baseSrc) throw new Error("Sin imagen base.");

          var baseIm = await loadImage(baseSrc);

          var canvas = document.createElement("canvas");
          var ctx = canvas.getContext("2d");
          canvas.width  = baseIm.naturalWidth  || baseIm.width;
          canvas.height = baseIm.naturalHeight || baseIm.height;

          ctx.drawImage(baseIm, 0, 0, canvas.width, canvas.height);

          var logoEl = $("#spw_logo_preview");
          if(logoEl && logoEl.src){
            var baseEl = $("#spw_product_img");
            var bw = baseEl.clientWidth,  bh = baseEl.clientHeight;
            var br = baseEl.getBoundingClientRect();
            var lr = logoEl.getBoundingClientRect();

            var cx_norm = ((lr.left + lr.width/2)  - br.left) / bw;
            var cy_norm = ((lr.top  + lr.height/2) - br.top ) / bh;
            var w_norm  = lr.width / bw;

            var cx = cx_norm * canvas.width;
            var cy = cy_norm * canvas.height;
            var drawW = w_norm * canvas.width;

            var logoIm = await loadImage(logoEl.src);
            var ratio = (logoIm.naturalHeight || logoIm.height) / (logoIm.naturalWidth || logoIm.width);
            var drawH = drawW * ratio;

            var rot = (parseInt($val("spw_rotation","0"),10) * Math.PI)/180;

            ctx.save(); ctx.translate(cx, cy); ctx.rotate(rot);
            ctx.drawImage(logoIm, -drawW/2, -drawH/2, drawW, drawH);
            ctx.restore();
          }

          return canvas.toDataURL("image/png");
        }

        async function postJSON(url, payload){
          try{
            var resp = await fetch(url,{
              method:"POST",
              headers:{ "Content-Type":"application/json", "X-Requested-With":"XMLHttpRequest" },
              credentials:"same-origin",
              body: JSON.stringify(payload),
            });
            var txt = await resp.text();
            var data=null; try{ data = JSON.parse(txt); }catch(_){}
            return {ok: !!(data && (data.ok===true || data.ok==="true")), data:data, status:resp.status, raw:txt};
          }catch(e){ return {ok:false, data:null, status:0, raw:String(e)}; }
        }
        async function postForm(url, payload){
          try{
            var fd = new FormData();
            Object.keys(payload||{}).forEach(function(k){ fd.append(k, payload[k]); });
            var resp = await fetch(url, { method: "POST", body: fd, credentials:"same-origin" });
            var txt = await resp.text();
            var data=null; try{ data = JSON.parse(txt); }catch(_){}
            return {ok: resp.ok && (!!data ? (data.ok===true || data.ok==="true") : true), data:data||{}, status:resp.status, raw:txt};
          }catch(e){ return {ok:false, data:null, status:0, raw:String(e)}; }
        }

        async function addToCart(){
          var variantId=($val("spw_variant_id","")||"").trim();
          var qty=parseInt($val("spw_qty","1"),10)||1;
          var tech=( (document.querySelector("input[name='spw_tech']:checked")||{}).value )||"";
          var svgColor=( (document.querySelector("input[name='spw_svg_color']:checked")||{}).value )||"";
          var notes=($val("spw_notes","")||"").trim();
          if(!variantId){ alert("Falta la variante del producto."); return; }

          var pngB64="";
          try{
            var du = await renderPNGDataURL();
            sessionStorage.setItem('spw_last_product_id', String(variantId));
            sessionStorage.setItem('spw_last_png', du || '');
            sessionStorage.setItem('spw_last_color', svgColor || '');
            pngB64 = du ? du.split('base64,')[1] : "";
          }catch(_){}

          // 1) meta JSON -> fallback HTTP
          var r1 = await postJSON("/spw/add_to_cart_meta", {
            variant_id: parseInt(variantId,10), qty: qty, tech: tech, svg_color: svgColor, notes: notes
          });
          if(!r1.ok){ r1 = await postForm("/spw/add_to_cart_meta_http", {
            variant_id: variantId, qty: qty, tech: tech, svg_color: svgColor, notes: notes
          }); }
          if(!r1.ok){ alert((r1.data && r1.data.message) ? r1.data.message : "No se pudo añadir al carrito."); return; }

          try { if (r1.data && r1.data.line_id) sessionStorage.setItem('spw_last_line_id', String(r1.data.line_id)); } catch(_){}

          // 2) PNG JSON -> fallback HTTP
          if(pngB64){
            var r2 = await postJSON("/spw/attach_png", { line_id: r1.data.line_id, png_b64: pngB64 });
            if(!r2.ok){ await postForm("/spw/attach_png_http", { line_id: r1.data.line_id, png_b64: pngB64 }); }
          }

          window.location.href = (r1.data && r1.data.cart_url) || "/shop/cart";
        }

        // ===== iOS Safari: descarga por POST; si no existe, abrir en misma pestaña para guardar
        async function downloadPNGiOSSafe(){
          try{
            var dataUrl = await renderPNGDataURL();
            var isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);
            var isSafari = /^((?!chrome|android).)*safari/i.test(navigator.userAgent);

            if (isIOS && isSafari) {
              // Intento 1: endpoint POST (si está disponible en el servidor)
              var ok = false;
              try {
                var form = document.createElement("form");
                form.method = "POST";
                form.action = "/spw/download_png";
                form.target = "_blank";
                var inp = document.createElement("input");
                inp.type = "hidden";
                inp.name = "png_b64";
                inp.value = dataURLtoBase64(dataUrl);
                form.appendChild(inp);
                document.body.appendChild(form);
                form.submit();
                ok = true;
                setTimeout(function(){ form.remove(); }, 1200);
              } catch(_e) { ok = false; }
              // Intento 2: abrir la imagen en esta pestaña (el usuario puede pulsar/guardar)
              if (!ok) {
                window.location.href = dataUrl;
              }
            } else {
              // Resto de navegadores
              var a = document.createElement("a");
              a.href = dataUrl; a.download = "personalizacion.png";
              if (typeof a.download === "undefined") {
                window.open(dataUrl, "_blank");
              } else {
                document.body.appendChild(a); a.click(); a.remove();
              }
            }
          }catch(_){ alert("No se pudo generar el PNG."); }
        }

        function init(){
          var logoInput=document.getElementById("spw_logo_input");
          var logoEl=document.getElementById("spw_logo_preview");
          if(logoInput){
            logoInput.addEventListener("change", async function(ev){
              var f = ev.target.files && ev.target.files[0]; if(!f) return;
              var name=(f.name||"").toLowerCase();
              S.isSVG = name.endsWith(".svg");
              var dataURL = await readAsDataURL(f);
              S.logoDataURL = dataURL;
              logoEl.src = dataURL;
              applyTransform();
            });
          }
          ["spw_size","spw_pos_x","spw_pos_y","spw_rotation"].forEach(function(id){
            var el=document.getElementById(id); if(el) el.addEventListener("input", applyTransform);
          });
          window.spwReset=function(){
            document.getElementById("spw_size").value=100;
            document.getElementById("spw_pos_x").value=0;
            document.getElementById("spw_pos_y").value=10;
            document.getElementById("spw_rotation").value=0;
            applyTransform();
          };

          var dl=document.getElementById("spw_download_png"); if(dl){ dl.addEventListener("click", function(){ downloadPNGiOSSafe(); }); }
          var add=document.getElementById("spw_add_to_cart"); if(add){ add.addEventListener("click", addToCart); }

          applyTransform();
        }

        if(document.readyState==="loading"){ document.addEventListener("DOMContentLoaded", init); } else { init(); }
      })();
      ]]></script>
    </t>
  </template>

  <!-- ============ INYECCIÓN UNIVERSAL EN website.layout (preview por línea + swatch color, sin duplicados) ============ -->
  <template id="spw_layout_cart_preview_inject" inherit_id="website.layout" name="SPW - Cart Preview Inject" priority="99">
    <xpath expr="//body" position="inside">
      <script type="text/javascript"><![CDATA[
        (function(){
          "use strict";
          function q(sel, root){return (root||document).querySelector(sel);}
          function qa(sel, root){return Array.from((root||document).querySelectorAll(sel));}

          // -------- Helpers de línea --------
          function parseLineIdFromHref(node){
            try{
              var a = node.closest('a[href*="line_id="]') || node.querySelector('a[href*="line_id="]');
              if(!a) return null;
              var u = new URL(a.getAttribute('href'), location.origin);
              return u.searchParams.get('line_id');
            }catch(_){ return null; }
          }
          function getLineId(line){
            if(!line) return null;
            return line.getAttribute('data-line-id')
                || ((q('input[name="line_id"]', line)||{}).value)
                || parseLineIdFromHref(line)
                || null;
          }
          function findLines(){
            // Cubrimos distintas maquetaciones de Odoo
            var sel = ".js_cart_lines .row, .o_wsale_cart_item, tr.js_cart_line, [data-line-id], .card.js_cart_item";
            return qa(sel).filter(function(n){ return !!getLineId(n); });
          }
          function holderFor(line){
            // Contenedor junto a los botones +/- (varía por versión/tema)
            return q(".o_wsale_cart_quantity", line)
                || q("div[name='quantity']", line)
                || q(".css_quantity", line)
                || q(".input-group-append", line)
                || null;
          }

          // -------- Color: convertir texto '#RRGGBB' en 'swatch' visual --------
          var COLOR_NAMES = {
            "#000000":"Negro","#FFFFFF":"Blanco","#D62828":"Rojo","#2060FF":"Azul","#2A9D8F":"Verde"
          };
          function enhanceColorText(root){
            try{
              qa("*", root).forEach(function(node){
                if(!node.childNodes || !node.childNodes.length) return;
                node.childNodes.forEach(function(c){
                  if(c.nodeType === 3){ // texto
                    var t = (c.nodeValue||"").trim();
                    // Coincide con 'Color SVG: #xxxxxx' o similar
                    var m = t.match(/(Color\s*SVG:?)\s*(#[0-9A-Fa-f]{6})/);
                    if(m){
                      var hex = m[2].toUpperCase();
                      var wrap = document.createElement("span");
                      wrap.style.display = "inline-flex";
                      wrap.style.alignItems = "center";
                      wrap.style.gap = "6px";

                      var label = document.createElement("span");
                      label.textContent = m[1] + " ";

                      var dot = document.createElement("span");
                      dot.style.display = "inline-block";
                      dot.style.width = "14px";
                      dot.style.height = "14px";
                      dot.style.borderRadius = "999px";
                      dot.style.border = "1px solid #e5e7eb";
                      dot.style.background = hex;

                      var name = document.createElement("span");
                      name.textContent = COLOR_NAMES[hex] ? (COLOR_NAMES[hex]+" ("+hex+")") : hex;

                      wrap.appendChild(label);
                      wrap.appendChild(dot);
                      wrap.appendChild(name);

                      var frag = document.createDocumentFragment();
                      frag.appendChild(wrap);

                      // Reemplaza solo ese trozo de texto
                      var before = c.nodeValue.split(m[0])[0] || "";
                      var after  = c.nodeValue.split(m[0])[1] || "";
                      var partsFrag = document.createDocumentFragment();
                      if(before){ partsFrag.appendChild(document.createTextNode(before)); }
                      partsFrag.appendChild(frag);
                      if(after){ partsFrag.appendChild(document.createTextNode(after)); }
                      node.replaceChild(partsFrag, c);
                    }
                  }
                });
              });
            }catch(_){}
          }

          // -------- Inserción del preview --------
          function insertPreview(line, src, lineId){
            // Evitar duplicados
            qa('.spw-preview[data-line="'+lineId+'"]', line).forEach(function(im){ im.remove(); });

            var img = new Image();
            img.className = "spw-preview";
            img.setAttribute("data-line", lineId);
            img.alt = "Personalización";
            img.loading = "lazy";
            img.src = src;
            img.style.maxWidth = "110px";
            img.style.height = "auto";
            img.style.border = "1px solid #e5e7eb";
            img.style.borderRadius = "6px";
            img.style.marginTop = "6px";
            img.style.marginLeft = "8px";
            img.onerror = function(){ img.remove(); };

            var h = holderFor(line);
            if (h && h.parentElement) {
              h.insertAdjacentElement('afterend', img);
            } else {
              line.appendChild(img);
            }
          }

          function trySessionPreview(line, lineId){
            try{
              var lastId = sessionStorage.getItem('spw_last_line_id');
              var dataUrl = sessionStorage.getItem('spw_last_png');
              if (lastId && dataUrl && String(lastId) === String(lineId)) {
                insertPreview(line, dataUrl, lineId);
                return true;
              }
            }catch(_){}
            return false;
          }

          function tryServerPreview(line, lineId){
            var url = "/spw/line_preview/" + encodeURIComponent(lineId) + ".png?_=" + Date.now();
            insertPreview(line, url, lineId);
          }

          var schedulePending = false;
          function inject(){
            if (!/\/shop\/cart/.test(location.pathname)) return;
            try{
              var lines = findLines();
              // Limpia huérfanos globales
              qa(".spw-preview").forEach(function(im){
                var lid = im.getAttribute("data-line");
                var keep = lines.some(function(l){ return getLineId(l) === lid; });
                if(!keep) im.remove();
              });
              lines.forEach(function(line){
                var id = getLineId(line);
                if (!id) return;
                // Color visual
                enhanceColorText(line);
                // Preview (primero sesión, luego servidor)
                if (!trySessionPreview(line, id)) {
                  tryServerPreview(line, id);
                }
              });
            }catch(_){}
          }
          function schedule(){
            if (schedulePending) return;
            schedulePending = true;
            requestAnimationFrame(function(){ schedulePending = false; inject(); });
          }

          function boot(){
            if (!/\/shop\/cart/.test(location.pathname)) return;
            inject();
            var root = q(".js_cart_lines") || q(".o_wsale_cart") || q("main") || document.body;
            if (window.MutationObserver && root){
              var mo = new MutationObserver(schedule);
              mo.observe(root, {childList:true, subtree:true});
            }
            setTimeout(inject, 400);
            setTimeout(inject, 1200);
          }

          if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', boot);
          } else {
            boot();
          }
        })();
      ]]></script>
    </xpath>
  </template>

</odoo>
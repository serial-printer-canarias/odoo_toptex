/** Minimal page helper to ensure website editor metadata is fine */
document.addEventListener("DOMContentLoaded", () => {
  // No requiere lógica ahora; se reserva por si en el futuro
  // pasamos datos al carrito junto con la personalización.
});